name: PR Slack - Notify New PR

on:
  pull_request:
    types: [opened, ready_for_review, synchronize]
  workflow_dispatch:
    inputs:
      pr_number:
        description: "PR number to test"
        required: true
        default: "1"

jobs:
  notify-slack:
    runs-on: ubuntu-latest
    if: >
      github.event_name == 'workflow_dispatch' ||
      github.event.pull_request.draft == false
    permissions:
      pull-requests: read
      contents: read
      models: read

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Check PR is still open
        id: check
        env:
          GH_TOKEN: ${{ secrets.GH_PAT }}
        run: |
          PR_NUMBER="${{ github.event.pull_request.number || inputs.pr_number }}"
          PR_STATE=$(gh api repos/${{ github.repository }}/pulls/$PR_NUMBER --jq '.state')
          echo "pr_state=$PR_STATE" >> $GITHUB_OUTPUT

      - name: Fetch PR data
        if: steps.check.outputs.pr_state == 'open'
        id: pr
        env:
          GH_TOKEN: ${{ secrets.GH_PAT }}
        run: |
          PR_NUMBER="${{ github.event.pull_request.number || inputs.pr_number }}"
          PR_JSON=$(gh api repos/${{ github.repository }}/pulls/$PR_NUMBER)
          echo "title=$(echo "$PR_JSON" | jq -r '.title')"           >> $GITHUB_OUTPUT
          echo "author=$(echo "$PR_JSON" | jq -r '.user.login')"     >> $GITHUB_OUTPUT
          echo "branch=$(echo "$PR_JSON" | jq -r '.head.ref')"       >> $GITHUB_OUTPUT
          echo "url=$(echo "$PR_JSON" | jq -r '.html_url')"          >> $GITHUB_OUTPUT
          echo "sha=$(echo "$PR_JSON" | jq -r '.head.sha')"          >> $GITHUB_OUTPUT
          echo "base=$(echo "$PR_JSON" | jq -r '.base.ref')"         >> $GITHUB_OUTPUT
          echo "commit_msg=$(echo "$PR_JSON" | jq -r '.title')"      >> $GITHUB_OUTPUT

      - name: Get diff stats
        if: steps.check.outputs.pr_state == 'open'
        id: diff
        run: |
          BASE=${{ steps.pr.outputs.base }}
          git fetch origin $BASE
          FILES=$(git diff --name-only origin/${BASE}...origin/${{ steps.pr.outputs.branch }} | wc -l | tr -d ' ')
          echo "file_count=$FILES" >> $GITHUB_OUTPUT
          git diff origin/${BASE}...origin/${{ steps.pr.outputs.branch }} -- '*.ts' '*.tsx' '*.js' '*.jsx' | head -c 3000 > /tmp/diff_snippet.txt

      - name: Generate AI summary in Vietnamese
        if: steps.check.outputs.pr_state == 'open'
        id: ai_summary
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          python3 - <<'PYEOF'
          import os, sys, json, urllib.request

          diff = open("/tmp/diff_snippet.txt").read() or "(no diff)"
          payload = {
            "model": "gpt-4o-mini",
            "max_tokens": 512,
            "messages": [{
              "role": "user",
              "content": f"Tóm tắt ngắn gọn những thay đổi trong PR này bằng tiếng Việt (tối đa 3 câu). Diff:\n{diff}"
            }]
          }
          req = urllib.request.Request(
            "https://models.inference.ai.azure.com/chat/completions",
            data=json.dumps(payload).encode(),
            headers={
              "Authorization": f"Bearer {os.environ['GITHUB_TOKEN']}",
              "Content-Type": "application/json"
            }
          )
          try:
            with urllib.request.urlopen(req, timeout=30) as resp:
              data = json.load(resp)
              summary = data["choices"][0]["message"]["content"].strip()
          except Exception as e:
            print(f"GitHub Models error: {e}", file=sys.stderr)
            summary = "Không thể tạo tóm tắt tự động."

          delimiter = "EOF_SUMMARY"
          with open(os.environ["GITHUB_OUTPUT"], "a") as f:
            f.write(f"summary<<{delimiter}\n{summary}\n{delimiter}\n")
          PYEOF

      - name: Post to Slack
        if: steps.check.outputs.pr_state == 'open'
        env:
          SLACK_BOT_TOKEN: ${{ secrets.SLACK_BOT_TOKEN }}
          SLACK_CHANNEL_ID: ${{ secrets.SLACK_CHANNEL_ID }}
          AI_SUMMARY: ${{ steps.ai_summary.outputs.summary }}
        run: |
          python3 - <<'PYEOF'
          import os, json, urllib.request

          pr_number   = "${{ github.event.pull_request.number || inputs.pr_number }}"
          pr_title    = "${{ steps.pr.outputs.title }}"
          pr_author   = "${{ steps.pr.outputs.author }}"
          pr_branch   = "${{ steps.pr.outputs.branch }}"
          pr_url      = "${{ steps.pr.outputs.url }}"
          file_count  = "${{ steps.diff.outputs.file_count }}"
          ai_summary  = os.environ.get("AI_SUMMARY", "Không thể tạo tóm tắt tự động.")
          commit_sha  = "${{ steps.pr.outputs.sha }}"[:7]
          commit_msg  = "${{ steps.pr.outputs.commit_msg }}" or "${{ steps.pr.outputs.sha }}"
          commit_url  = "${{ steps.pr.outputs.url }}/commits/${{ steps.pr.outputs.sha }}"
          event_type  = "${{ github.event_name }}"
          action      = "${{ github.event.action }}"

          event_label = {
            ("pull_request", "opened"): "🆕 PR mới được tạo",
            ("pull_request", "synchronize"): "🔄 Có commit mới",
            ("pull_request", "ready_for_review"): "✅ Sẵn sàng review",
          }.get((event_type, action), "📋 PR cập nhật")

          blocks = [
            {
              "type": "header",
              "text": {"type": "plain_text", "text": "Pull Request can review"}
            },
            {
              "type": "section",
              "fields": [
                {"type": "mrkdwn", "text": f"*Tieu de:*\n<{pr_url}|{pr_title}>"},
                {"type": "mrkdwn", "text": f"*Tac gia:*\n{pr_author}"},
                {"type": "mrkdwn", "text": f"*Branch:*\n`{pr_branch}`"},
                {"type": "mrkdwn", "text": f"*So file thay doi:*\n{file_count}"},
              ]
            },
            {
              "type": "section",
              "fields": [
                {"type": "mrkdwn", "text": f"*Su kien:*\n{event_label}"},
                {"type": "mrkdwn", "text": f"*Commit:*\n<{commit_url}|`{commit_sha}`> {(commit_msg.splitlines() or [''])[0][:60]}"},
              ]
            },
            {
              "type": "section",
              "text": {"type": "mrkdwn", "text": f"*Tom tat AI:*\n{ai_summary}"}
            },
            {
              "type": "actions",
              "elements": [
                {
                  "type": "button",
                  "text": {"type": "plain_text", "text": "Xem PR"},
                  "url": pr_url,
                  "action_id": "view_pr"
                }
              ]
            },
            {
              "type": "context",
              "elements": [
                {"type": "mrkdwn", "text": f"Dung `/approve {pr_number}` de merge hoac `/reject {pr_number}` de tu choi"}
              ]
            }
          ]

          req = urllib.request.Request(
            "https://slack.com/api/chat.postMessage",
            data=json.dumps({"channel": os.environ["SLACK_CHANNEL_ID"], "blocks": blocks}).encode(),
            headers={
              "Authorization": f"Bearer {os.environ['SLACK_BOT_TOKEN']}",
              "Content-Type": "application/json"
            }
          )
          with urllib.request.urlopen(req, timeout=15) as resp:
            result = json.load(resp)
            print("Slack response:", result.get("ok"), result.get("error", ""))
          PYEOF
