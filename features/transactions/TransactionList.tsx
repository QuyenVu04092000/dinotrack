export default function TransactionList() {
  // Data and business logic will be added later.
  return (
    <section aria-label="Transaction list placeholder">
      <p className="text-sm text-mutedForeground">
        Transaction list UI will be implemented from Figma. No data is loaded yet.
      </p>
    </section>
  );
}
