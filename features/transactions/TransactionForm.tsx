"use client";

export default function TransactionForm() {
  // Interaction and business logic will be added later.
  return (
    <form aria-label="Transaction form placeholder" className="space-y-4" onSubmit={(event) => event.preventDefault()}>
      <p className="text-sm text-mutedForeground">Transaction form UI and behavior will be implemented from Figma.</p>
    </form>
  );
}
